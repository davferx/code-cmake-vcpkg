{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build MsVc Dbg",
            "type": "shell",
            "command": "ninja",
            "options": {"cwd": "${workspaceFolder}/out/x64-msvc-dbg"},
            "problemMatcher": "$msCompile",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
